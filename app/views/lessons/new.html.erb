<h1>New Lesson</h1>

<%= form_for [@section, @lesson] do |f| %>
  <%= f.label "Lesson Title" %>
  <%= f.text_field :title %>

  <%= f.label "Lesson Content" %>
  <%= f.text_field :content %>

  <%= f.submit class: 'btn btn-success'%>
<% end %>
